public class Player {
    String playerId;
    int currPosition;

    public Player(String playerId, int currPosition) {
        this.playerId = playerId;
        this.currPosition = currPosition;
    }
}
